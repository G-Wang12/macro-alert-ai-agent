# Copy to .env and fill in your key
# xAI (Grok) API key
XAI_API_KEY=

# Optional: override if xAI changes endpoints
XAI_BASE_URL=https://api.x.ai/v1

# Optional: used for chat completions when you add them
GROK_MODEL=grok-2-latest
